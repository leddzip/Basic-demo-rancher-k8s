Kubernetes with Rancher on public network
=========================================

Only applying the command to add a new node to the cluster is enough.
Since each node as a distinct IP address on a public network, there is no
collision possible.